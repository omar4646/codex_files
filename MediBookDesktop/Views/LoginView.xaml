<UserControl x:Class="MediBookDesktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Border Width="420" Style="{StaticResource Card}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="28">
            <StackPanel>
                <TextBlock Text="MediBook Desktop" Style="{StaticResource TitleText}" />
                <TextBlock Text="Sign in to manage appointments." Style="{StaticResource MutedText}" Margin="0,0,0,18" />

                <TextBlock Text="Email" FontWeight="SemiBold" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Password" FontWeight="SemiBold" />
                <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordInput_OnPasswordChanged" />

                <Button Content="Login" Command="{Binding LoginCommand}" HorizontalAlignment="Stretch" Margin="0,8,0,4" />
                <Button Content="Create patient account" Command="{Binding ShowRegisterCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Stretch" Margin="0,4,0,0" />

                <TextBlock Text="{Binding Message}" Foreground="{StaticResource DangerBrush}" Margin="0,12,0,0" TextWrapping="Wrap" />
                <TextBlock Text="Default admin: admin@medibook.local / Admin123!" Style="{StaticResource MutedText}" Margin="0,16,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
