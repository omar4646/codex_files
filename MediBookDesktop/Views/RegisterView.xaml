<UserControl x:Class="MediBookDesktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Border Width="450" Style="{StaticResource Card}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="28">
            <StackPanel>
                <TextBlock Text="Create patient account" Style="{StaticResource TitleText}" />
                <TextBlock Text="Local accounts stay on this computer." Style="{StaticResource MutedText}" Margin="0,0,0,18" />

                <TextBlock Text="Full name" FontWeight="SemiBold" />
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Email" FontWeight="SemiBold" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Password" FontWeight="SemiBold" />
                <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordInput_OnPasswordChanged" />

                <Button Content="Register" Command="{Binding RegisterCommand}" HorizontalAlignment="Stretch" Margin="0,8,0,4" />
                <Button Content="Back to login" Command="{Binding BackToLoginCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Stretch" Margin="0,4,0,0" />

                <TextBlock Text="{Binding Message}" Foreground="{StaticResource DangerBrush}" Margin="0,12,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
